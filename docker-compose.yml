services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: donmateo
      POSTGRES_PASSWORD: donmateo
      POSTGRES_DB: donmateo
    ports:
      - "5432:5432"
    volumes:
      - donmateo_pg:/var/lib/postgresql/data

volumes:
  donmateo_pg:
